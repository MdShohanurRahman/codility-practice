# Topics

Interview study notes, one topic per file.

| Topic | File | Why it matters |
| :--- | :--- | :--- |
| Java concurrency, threading, and GC | [Java_Concurrency_Threading_and_GC.md](Java_Concurrency_Threading_and_GC.md) | EPAM asks threads and GC. Hong Leong calls out virtual threads in the JD. |

Start with the night-before sheet at the bottom of that note if time is short. Otherwise read in order: threads, locks, pools, `CompletableFuture`, concurrent collections, virtual threads, then JVM memory and GC.
